export class Endpoints {
  auth: string;
  users: string;
  workExperience: string;
  education: string;
  skills: string;
  icons: string;
}

export class AppInfo {
  message: string;
  description: string;
  notes: string;
  version: string;
  status: string;
  endpoints: Endpoints;
}
